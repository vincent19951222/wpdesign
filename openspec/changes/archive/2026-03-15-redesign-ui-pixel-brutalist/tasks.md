## 1. Design System Foundation

- [x] 1.1 更新 `tailwind.config.js` 与全局主题变量，加入像素风颜色令牌、字体变量和硬阴影配置
- [x] 1.2 在全局样式中接入 `Fusion Pixel`、`Press Start 2P`、`Silkscreen` 的当前运行时可用加载方案，并统一全局 `-webkit-font-smoothing: none`
- [x] 1.3 在全局样式中建立中文正文/标题、英文大标题、英文次级文本/按钮的字体映射规则
- [x] 1.4 在全局样式中提取 `.pixel-panel`、`.pixel-btn`、`.pixel-input` 等基础复用类，并定义按钮按下态与输入框聚焦态
- [x] 1.5 建立偶数像素尺寸基线，约束字号、margin 和 padding 使用偶数像素值

## 2. Shared Component Migration

- [x] 2.1 重构共享按钮组件及其变体，使主按钮、次按钮和工具按钮默认采用像素按钮基线
- [x] 2.2 重构共享输入类组件，使输入框、文本域和上传触发入口采用统一的像素输入框基线
- [x] 2.3 重构共享面板/卡片类容器样式，替换旧的混合 `neo-*` 阴影与边框实现
- [x] 2.4 审核共享组件中的字号、margin 和 padding，消除不符合偶数像素网格的尺寸定义

## 3. Core Page Refresh

- [x] 3.1 重构首页与主题选择区，统一背景、标题区、主题卡片和主要 CTA 的像素风表现
- [x] 3.2 重构编辑台主工作区，统一编辑容器、操作面板、工具区和辅助入口的像素风布局与样式
- [x] 3.3 重构预览区及相关操作区域，确保预览容器、复制入口和结果区采用统一的像素风面板与控件样式

## 4. Validation and Cleanup

- [x] 4.1 清理主路径页面和共享组件中明显残留的旧 `neo-*` 视觉实现，减少新旧风格混用
- [x] 4.2 验证 Markdown 编辑、主题切换、预览和复制等核心工作流在新界面下保持可用
- [x] 4.3 进行一次像素网格检查，确认核心页面与共享组件的字号、margin、padding 均使用偶数像素值
- [x] 4.4 运行构建并完成一次手动检查，确认样式重构未引入新的构建错误或关键视觉回归
